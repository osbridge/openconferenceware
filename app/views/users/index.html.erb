<% page_title "Users" %>

<div id="user-index">
  <table class="scaffolded">
    <tr>
      <% if admin? %>
        <th>Admin?</th>
      <% end %>
      <th>Name</th>
      <th>User ID</th>
    </tr>
    <% @users.each do |user| -%>
    <tr class="<%= cycle('odd', 'even') %>">
      <% if admin? %>
        <td><%= user.admin? %></td>
      <% end %>
      <td>
        <%= link_to(user.label, user_path(user)) %>
      </td>
      <td>
        <%= user.id %>
      </td>
    </tr>
    <% end -%>
  </table>
</div>
